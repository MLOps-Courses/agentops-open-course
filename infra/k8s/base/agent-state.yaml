apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agentops-agent-state
  namespace: agentops
  labels:
    app.kubernetes.io/name: agentops-agent
    app.kubernetes.io/part-of: agentops-open-course
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
