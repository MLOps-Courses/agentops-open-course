---
description: Give the agent long-term knowledge over the runbook library with keyword retrieval.
---

# 3.4. Memory

## What is the agent's memory here?

The Ops Copilot's long-term knowledge is a library of **runbooks** — `agents/data/runbooks/*.md`, one per failure mode (`high-latency`, `service-down`, `disk-full`, …). Each incident row carries a `runbook` slug linking it to the right procedure. Two knowledge tools let the agent reach that library:

- `get_runbook` — fetch one runbook by its **exact slug**, the fast path when an incident already names it.
- `search_runbooks` — a **keyword search** for when the slug is unknown, returning the most relevant runbooks first.

Keeping the knowledge base as plain Markdown files keeps the course fully offline and deterministic — a stepping stone to a real vector store, which we come back to at the end of this page.

## How does the agent fetch a known runbook?

By slug. An incident record includes its `runbook` field, so the direct lookup is usually all the agent needs:

=== "Python"

    ```python
    # agents/python/src/agent/memory.py
    def get_runbook(slug: str) -> dict[str, Any]:
        """Fetch a runbook by its exact slug (e.g. an incident's ``runbook`` field).

        Args:
            slug: The runbook identifier, e.g. ``high-latency`` or ``service-down``.

        Returns:
            A dict with the ``slug`` and its markdown ``content``, or an ``error`` if unknown.
        """
        content = data.read_runbook(slug)
        if content is None:
            known = ", ".join(data.list_runbook_slugs())
            return {"error": f"No runbook named {slug!r}. Available runbooks: {known}."}
        return {"slug": slug, "content": content}
    ```

=== "Go"

    ```go
    // agents/go/internal/memory/memory.go
    func getRunbookTool(store *data.Store) (tool.Tool, error) {
    	return functiontool.New(functiontool.Config{
    		Name:        "get_runbook",
    		Description: "Fetch a runbook by its exact slug (e.g. an incident's runbook field).",
    	}, func(_ agent.Context, in getRunbookInput) (map[string]any, error) {
    		content, ok, err := store.ReadRunbook(in.Slug)
    		if err != nil {
    			return nil, err
    		}
    		if !ok {
    			slugs, listErr := store.ListRunbookSlugs()
    			if listErr != nil {
    				return nil, listErr
    			}
    			return map[string]any{
    				"error": fmt.Sprintf("No runbook named %q. Available runbooks: %s.", in.Slug, strings.Join(slugs, ", ")),
    			}, nil
    		}
    		return map[string]any{"slug": in.Slug, "content": content}, nil
    	})
    }
    ```

When the slug is wrong, both return an error that **lists the available runbooks** — turning a dead end into a hint the model can act on next turn.

## How does keyword search rank runbooks?

When the model does not know the slug, `search_runbooks` scores every runbook against the query with **TF-IDF-style** weighting. Three ideas combine into one score:

1. **Keyword match**: split the query into lowercase terms, dropping very short ones (< 3 characters) that carry no signal, then count how often each term appears in a runbook.
1. **Inverse document frequency (IDF)**: weight each hit by `total_runbooks / runbooks_containing_the_term`, so a **rare** term (like `pool` or `latency`) counts far more than a **ubiquitous** one (like `service`) that appears everywhere and discriminates nothing.
1. **Slug boost**: if a term appears in the runbook's own slug, add a strong bonus — the slug names the failure mode, so a query term matching it is a very reliable signal.

Runbooks with a positive score are sorted best-first and the top `limit` are returned. It is deterministic and offline, yet good enough to route most free-text incident descriptions to the right procedure.

=== "Python"

    ```python
    # agents/python/src/agent/memory.py
    def search_runbooks(query: str, limit: int = 3) -> dict[str, Any]:
        """Search the runbook knowledge base for guidance relevant to a free-text query.

        Uses TF-IDF-style scoring: a term counts more when it is rare across runbooks (so
        ubiquitous words like "service" don't dominate), and a term matching a runbook's slug
        gets a strong boost. Deterministic and offline — a stepping stone to semantic RAG.

        Args:
            query: What you are trying to resolve, e.g. ``database connection pool exhausted``.
            limit: The maximum number of runbooks to return (most relevant first).

        Returns:
            A dict with ``count`` and a ``runbooks`` list (slug + markdown content), best match first.
        """
        if limit <= 0:  # a non-positive limit falls back to the default (matches the Go track)
            limit = 3
        terms = _terms(query)
        contents = {slug: (data.read_runbook(slug) or "") for slug in data.list_runbook_slugs()}
        total = len(contents) or 1
        # Document frequency: how many runbooks contain each distinct term.
        doc_freq = {term: sum(term in text.lower() for text in contents.values()) for term in set(terms)}

        scored: list[tuple[float, str, str]] = []
        for slug, content in contents.items():
            haystack = content.lower()
            score = 0.0
            for term in terms:
                frequency = doc_freq[term]
                if frequency:
                    score += haystack.count(term) * (total / frequency)  # rarer term → higher weight
                if term in slug:
                    score += total * 5  # the slug names the failure mode: a strong signal
            if score > 0:
                scored.append((score, slug, content))
        scored.sort(key=lambda row: row[0], reverse=True)
        top = scored[:limit]
        return {"count": len(top), "runbooks": [{"slug": slug, "content": content} for _, slug, content in top]}
    ```

=== "Go"

    ```go
    // agents/go/internal/memory/memory.go
    // searchRunbooks ranks runbooks with TF-IDF-style scoring: rarer terms weigh more (so
    // ubiquitous words like "service" don't dominate) and a term matching a runbook's slug
    // gets a strong boost.
    func searchRunbooks(store *data.Store, query string, limit int) (searchRunbooksOutput, error) {
    	if limit <= 0 {
    		limit = defaultLimit
    	}
    	slugs, err := store.ListRunbookSlugs()
    	if err != nil {
    		return searchRunbooksOutput{}, err
    	}
    	contents := make(map[string]string, len(slugs))
    	for _, slug := range slugs {
    		content, _, readErr := store.ReadRunbook(slug)
    		if readErr != nil {
    			return searchRunbooksOutput{}, readErr
    		}
    		contents[slug] = content
    	}

    	total := len(contents)
    	if total == 0 {
    		return searchRunbooksOutput{Count: 0, Runbooks: []runbook{}}, nil
    	}
    	queryTerms := terms(query)
    	docFreq := documentFrequency(queryTerms, contents)

    	type ranked struct {
    		slug  string
    		score float64
    	}
    	var results []ranked
    	for slug, content := range contents {
    		haystack := strings.ToLower(content)
    		var score float64
    		for _, term := range queryTerms {
    			if freq := docFreq[term]; freq > 0 {
    				score += float64(strings.Count(haystack, term)) * (float64(total) / float64(freq))
    			}
    			if strings.Contains(slug, term) {
    				score += float64(total) * 5 // the slug names the failure mode: a strong signal
    			}
    		}
    		if score > 0 {
    			results = append(results, ranked{slug: slug, score: score})
    		}
    	}
    	// Score descending, then slug ascending for deterministic ties.
    	sort.Slice(results, func(i, j int) bool {
    		if results[i].score != results[j].score {
    			return results[i].score > results[j].score
    		}
    		return results[i].slug < results[j].slug
    	})

    	out := searchRunbooksOutput{Runbooks: []runbook{}}
    	for i, r := range results {
    		if i >= limit {
    			break
    		}
    		out.Runbooks = append(out.Runbooks, runbook{Slug: r.slug, Content: contents[r.slug]})
    	}
    	out.Count = len(out.Runbooks)
    	return out, nil
    }
    ```

The two implementations are line-for-line equivalent in behavior. The Go version adds an explicit **tie-break** (slug ascending) so equal scores sort deterministically — worth doing when a map's iteration order is otherwise random.

## Why not use vector embeddings from the start?

Because keyword retrieval is the honest 80/20 baseline: zero dependencies, no model calls, fully reproducible in tests and evals. It teaches the _shape_ of retrieval — score, rank, return the top `k` and feed them to the model — without a vector database or an embedding provider in the loop.

It is deliberately a **stepping stone**. The tool interface (`search_runbooks(query, limit) → ranked runbooks`) is exactly what a semantic retriever exposes, so upgrading is a swap behind a stable contract:

- Replace the TF-IDF scorer with **vector similarity** over embedded runbook chunks for true semantic recall.
- Or hand memory to the ADK **`MemoryService`**, which manages ingestion, retrieval, and cross-session recall for you.

Both are drop-in replacements for the ranking function — the agent, the tools' signatures, and the evals stay put. Next, chain these tools into a fixed pipeline with [3.5. Workflows](./3.5. Workflows.md).
