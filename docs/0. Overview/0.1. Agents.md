---
description: What an AI agent is, common agentic patterns, and when not to use one.
---

# 0.1. Agents

## What is an AI agent?

An **AI agent** is a program that uses a language model to decide, step by step, how to reach a goal — and that can take actions in the world through **tools**. The model is the reasoning engine; the tools are its hands. Unlike a fixed script, an agent chooses _which_ action to take next based on the goal, the conversation so far, and the results of its previous actions.

A useful working definition: an agent is a **model** that runs in a **loop**, calling **tools** and reading their results, until the task is done. The Ops Copilot you build in this course is an agent in exactly this sense — given "investigate INC-002", it decides to look up the incident, read the relevant runbook, check the service status, and propose a fix.

## What is the agentic loop?

The agentic loop is the cycle at the heart of every agent. Three ingredients drive it:

1. **Model**: the LLM that reasons about the goal and decides the next step.
1. **Tools**: functions the model can call to observe or change the world (query a database, read a runbook, restart a service).
1. **Memory**: the context the model carries between steps — the running conversation (short-term) and any retrieved knowledge or past sessions (long-term).

Each turn, the model reads the current context, decides whether to answer or call a tool, receives the tool result, and loops again until it produces a final answer.

```mermaid
flowchart LR
    Goal([Goal]) --> Model
    Model -->|call tool| Tools
    Tools -->|result| Memory
    Memory --> Model
    Model -->|done| Answer([Answer])
```

The framework — Google ADK, in this course — owns this loop for you: it manages sessions, formats tool calls, runs the tools, and feeds results back to the model.

## What are the common agentic patterns?

Most agents are built from a few reusable patterns, which the course teaches one at a time:

- **Tool use**: the model calls functions to fetch data or act. This is the foundation — an agent with no tools is just a chatbot. Covered in [3.1. Tools](../3. Capabilities/3.1. Tools.md).
- **Reflection**: the agent reviews its own output and revises it, often in a loop until a quality bar is met. Useful for drafting, checking, and self-correction.
- **Planning**: the agent breaks a goal into ordered sub-steps before executing, rather than reacting one step at a time. Helpful for multi-stage tasks like `triage → diagnose → recommend`.
- **Multi-agent**: several specialized agents collaborate, one delegating to another. In the Ops Copilot, a triage agent delegates to a diagnosis agent over [A2A](../3. Capabilities/3.6. A2A.md).

These patterns compose. A capable agent typically uses tools, reflects on results, and may delegate parts of the work to sub-agents.

## What is the difference between an agent and a workflow?

A **workflow** runs a fixed, predefined sequence of steps — you decide the control flow. An **agent** decides the control flow itself, at run time, using the model. Workflows are predictable and cheap; agents are flexible but non-deterministic and more expensive.

The two are not mutually exclusive. ADK 2.0 offers both classic **workflow agents** (Sequential, Parallel, Loop) and a **graph-based runtime**, so you can wrap deterministic steps around model-driven ones. (This course builds on the graph runtime, where the classic Sequential/Parallel/Loop agents are now deprecated — see [3.5. Workflows](../3. Capabilities/3.5. Workflows.md).) Reach for the model's autonomy only where the flexibility actually pays off.

## When should you not use an agent?

Agents add latency, cost, and unpredictability. Prefer a simpler solution when:

- **The steps are known and fixed**: a script, a workflow, or plain code is cheaper and more reliable than asking a model to re-derive the plan every time.
- **Correctness is non-negotiable and mechanical**: for deterministic transforms, validation, or math, call the function directly instead of hoping the model routes to it.
- **A single model call suffices**: if one prompt answers the question, you do not need a loop of tool calls.
- **The cost of a wrong action is high and unguarded**: never let an agent take irreversible actions without validation and human approval — see [4.5. Guardrails](../4. Quality/4.5. Guardrails.md).

The pragmatic rule: use an agent when the task genuinely requires _deciding_ what to do next from context. Otherwise, write the simpler thing.
