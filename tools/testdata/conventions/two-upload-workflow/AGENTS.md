# Fixture repository

The organization caps artifact and log retention at **7 days**; this fixture exists so the retention contract can be exercised against a workflow whose upload steps and retention values do not pair up.
