# Rejected Skaffold runbook

Both spellings below send a learner to the wrong working directory or the retired short profile flag, and both are what the rule exists to catch on a course page.

```bash
skaffold dev --filename infra/skaffold.yaml --profile local
skaffold run -p gke
```
